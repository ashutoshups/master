## Server (SSR / Express) environment variables
#
# Copy this file to `.env` and fill in values.
# This file is safe to commit; `.env` should NOT be committed.
#

# AWS config used by the presign endpoint in `server.ts`
AWS_REGION=ap-south-1
S3_BUCKET_NAME=my-from-profile-bucket

# Credentials for local development only (prefer IAM roles in production)
AWS_ACCESS_KEY_ID=AKIAURLDDRJCWGMUUWPP
AWS_SECRET_ACCESS_KEY=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

